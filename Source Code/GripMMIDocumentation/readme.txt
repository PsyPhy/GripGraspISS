========================================================================
    MAKEFILE PROJECT : GripMMIDocumentation Project Overview
========================================================================

We keep the documentation files in the source code directory tree so that
they are subject to source control. The makefile currently just copies
those files to the appropriate directory in the execution environment.
